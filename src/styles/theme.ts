import { DefaultTheme } from 'styled-components/native';
import colors from './colors';
import fonts from './fonts';

export const moneyMapTheme: DefaultTheme = {
  colors: { ...colors },
  fonts: { ...fonts }
};